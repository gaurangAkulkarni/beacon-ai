[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "beacon-ai"
version = "0.1.0"
description = "MLX-first LLM inference for Apple Silicon"
requires-python = ">=3.9"
license = "Apache-2.0"

[tool.maturin]
features = ["pyo3/extension-module"]
